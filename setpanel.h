#ifndef SETPANEL_H
#define SETPANEL_H

#include <QWidget>

class setpanel : public QWidget
{
    Q_OBJECT
public:
    explicit setpanel(QWidget *parent = 0);
    ~setpanel();

signals:

public slots:
};

#endif // SETPANEL_H
